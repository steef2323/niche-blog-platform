/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'v5.airtableusercontent.com',
      'dl.airtable.com',
      'airtable.com'
    ],
  },
};

module.exports = nextConfig; 