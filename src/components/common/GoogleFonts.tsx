/**
 * Font CSS variables are set in the root layout (self-hosted Inter via next/font).
 * This client component is kept as a no-op so older imports do not re-inject
 * render-blocking Google Fonts stylesheets.
 */
export default function GoogleFonts() {
  return null;
}
